{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    // be explicit about types included
    // to avoid clashing with Jest types
    "types": ["cypress", "node"]
  },
  "include": ["./**/*.ts", "../postinstall.js"],
  "exclude": ["node_modules", "**/*.spec.tsx", "**/*.spec.ts"]
}
